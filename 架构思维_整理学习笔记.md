# 架构思维学习笔记

## 1. 这份笔记要解决什么问题

很多 Qt 客户端代码一开始都能跑，但随着页面变复杂、状态变多、异步任务增多、外部进程接入变多，代码会迅速失控。  
所谓“架构思维”，本质上不是追求名词，而是训练自己持续回答下面这些问题：

- 谁负责什么
- 什么时候创建、运行、结束、清理
- 状态由谁修改，怎么流转
- 以后增加第二个、第三个同类功能时怎么办
- 出问题后如何快速定位

可以把架构看成两个维度：

### 空间维度

回答“谁负责什么”，对应：

- 分层
- 模块边界
- 模块职责

### 时间维度

回答“当前处于什么阶段”，对应：

- 状态管理
- 生命周期管理
- 流程流转

---

## 2. 架构思维的核心能力地图

### 2.1 UI 层和业务层分离

不要把下面这些内容全塞进 `MainWindow` / `QWidget`：

- UI 控件逻辑
- 业务逻辑
- 文件读写
- 网络请求
- 进程控制
- 缓存状态

更合理的目标是逐步形成这样的职责拆分：

- `View`：界面展示、控件交互、信号转发
- `ViewModel / Presenter / Workflow`：业务流程、状态驱动、调度逻辑
- `Service`：进程、文件、网络、系统能力
- `Repository / DataSource`：数据访问、配置读取、缓存读写

这里的重点不是一定要用哪套术语，而是要有“界面不直接吞掉全部职责”的意识。

### 2.2 状态管理

Qt 客户端很容易写成“点一下改一点，全靠成员变量撑”。  
状态管理解决的是：页面当前到底处于什么状态，以及谁有资格修改它。

一个工具页至少可能有这些状态：

- 未初始化
- 加载中
- 就绪
- 执行中
- 失败
- 已关闭

你要重点回答 4 个问题：

- 当前有哪些状态
- 状态如何流转
- 谁能改状态
- 状态变化后 UI 怎么同步

### 2.3 生命周期管理

很多客户端问题本质不是功能写错，而是对象和任务“活得不清楚”。

要重点训练这些生命周期：

- 页面创建 / 显示 / 隐藏 / 销毁
- 子进程启动 / ready / 退出
- socket 连接 / 断开 / 重连
- 模型加载 / 释放
- 后台任务开始 / 取消 / 完成

生命周期管理本质上只管 4 件事：

- 什么时候创建
- 什么时候开始工作
- 什么时候结束
- 结束后怎么清理

### 2.4 模块边界

分层解决“这个类大概属于哪一层”；边界解决“它虽然在这一层，但到底能做什么、不能做什么”。

例如：

- `Page` 属于 UI 层
- `Workflow` 属于业务层
- `ProcessService` 属于服务层

但更重要的是边界约束：

`Workflow` 可以：

- 改状态
- 调 `Service`
- 发结果

`Workflow` 不可以：

- 直接改按钮
- 直接弹框

`Service` 可以：

- 启动进程
- 读写文件
- 返回错误

`Service` 不可以：

- 决定页面怎么提示用户
- 决定 tab 怎么显示

一句话总结：  
分层是结构，边界是职责约束。

### 2.5 可扩展设计

每次写功能都要敏感地意识到未来变化方向，例如：

- 以后会不会加第二个同类功能
- 以后会不会多开
- 以后会不会插件化
- 以后会不会替换底层实现

注意：  
可扩展设计不是一开始就过度抽象，而是对未来高概率变化点保持警觉。

### 2.6 可观测性

成熟客户端不是“只会跑”，还要“出了问题能查”。

最基本的可观测性建设包括：

- 日志分层
- 错误码
- 状态提示
- 调试开关
- 性能采样点

可以把可观测性分成三层理解：

1. 打关键日志
2. 日志 + 状态可见
3. 日志 + 状态 + 错误上下文 + 指标

你现在先掌握第一层和第二层，已经很有价值。

---

## 3. 设计时要反复问自己的问题

当你开始稳定考虑下面这些问题，就已经开始进入架构视角：

- 多开怎么办
- 崩溃恢复怎么办
- 日志怎么收
- 任务取消怎么办
- 内存怎么回收
- 大文件怎么加载
- 卡顿怎么定位

这些问题不会直接让页面“多一个按钮”，但会决定系统能不能长期维护。

---

## 4. UI 层和业务层分离应该怎么渐进式推进

不是所有页面一上来都要拆成三四层。  
当复杂度还不够时，抽象就是负担；当变化已经开始冲击边界时，不抽象就是技术债。

推荐按复杂度逐步演进：

### 阶段 A：一个页面类

适合：

- 小页面
- 简单逻辑

特点：

- UI 和少量简单逻辑可以先放一起

### 阶段 B：页面类 + 私有函数

适合：

- 槽函数开始变长
- 但逻辑还集中在一个页面里

特点：

- 先把重复逻辑、分支逻辑抽成私有函数

### 阶段 C：页面类 + Request / Result 数据对象

适合：

- 输入输出开始复杂
- 需要先把参数和结果收口

特点：

- 先抽数据结构，而不是急着抽大类

### 阶段 D：页面类 + 流程对象

适合：

- 多步骤
- 多状态
- 明显存在流程控制

特点：

- 开始把“点按钮 -> 校验 -> 执行 -> 回收 -> 更新状态”这一串流程挪出页面

### 阶段 E：页面类 + 流程对象 + Service

适合：

- 执行逻辑本身也复杂
- 有独立复用价值
- 依赖底层资源较多

特点：

- 页面负责交互
- 流程对象负责编排
- Service 负责底层能力

最复杂的典型形态可以理解为：

- UI 层
- 业务层
- 服务层

---

## 5. 状态管理怎么真正落地

状态管理建议按下面 5 步做。

### 5.1 枚举化

先明确“当前状态是什么”，不要靠多个布尔值散落表达。

```cpp
enum class TaskState
{
    Idle,
    Running,
    Finished,
    Failed
};
```

### 5.2 单一入口改状态

不要让任何地方都能随手改状态。  
最好只有一个入口，比如：

```cpp
void Workflow::setState(TaskState state)
{
    if (m_state == state) {
        return;
    }
    m_state = state;
    emit stateChanged(m_state);
}
```

### 5.3 UI 通过信号响应状态

状态变化后，由 UI 统一响应并刷新：

```cpp
switch (state) {
case TaskState::Idle:
    ui->startButton->setEnabled(true);
    break;
case TaskState::Running:
    ui->startButton->setEnabled(false);
    break;
case TaskState::Finished:
case TaskState::Failed:
    ui->startButton->setEnabled(true);
    break;
}
```

### 5.4 限制合法流转

不是所有状态都能任意跳转。  
例如：

- `Idle -> Running` 可以
- `Running -> Finished` 可以
- `Finished -> Running` 未必合理

可以用单独函数约束：

```cpp
bool canTransit(PageState from, PageState to)
{
    switch (from) {
    case PageState::Idle:
        return to == PageState::Loading || to == PageState::Closed;
    case PageState::Loading:
        return to == PageState::Ready || to == PageState::Failed;
    case PageState::Ready:
        return to == PageState::Loading || to == PageState::Closed;
    case PageState::Failed:
        return to == PageState::Loading || to == PageState::Closed;
    case PageState::Closed:
        return false;
    }
    return false;
}
```

### 5.5 多维状态拆分

复杂页面通常不只有一个状态维度。  
例如同时存在：

- `sessionState`
- `socketState`
- `taskState`

UI 更新有两种常见方式：

1. 分别更新  
   例如 `sessionStateChanged` 更新主按钮，`socketStateChanged` 更新连接标识，`taskStateChanged` 更新进度区。

2. 组合判断  
   某些按钮或区域依赖多个状态共同决定：

```cpp
bool canOperate =
    m_sessionState == SessionState::Ready &&
    m_socketState == SocketState::Connected &&
    m_taskState != TaskState::Running;
```

这一步的价值在于：  
你不会再把“按钮能不能点”这种规则散在几十个槽函数里。

---

## 6. 生命周期管理怎么理解

### 6.1 一个典型的反例

```cpp
void onStartClicked()
{
    ExportWorker *worker = new ExportWorker;
    connect(worker, &ExportWorker::progressChanged,
            this, [this](int v) {
                m_label->setText(QString("进度 %1%").arg(v));
            });
    connect(worker, &ExportWorker::finished,
            this, [this]() {
                m_label->setText("导出完成");
            });
    worker->start();
}
```

这段代码能跑，但生命周期是失控的。主要问题有：

1. `worker` 谁释放  
2. 页面关闭后回调打到哪里  
3. 连点两次会不会产生多个任务  
4. 导出过程能不能取消  
5. 页面销毁时任务是否需要终止

这就是典型的“对象创建了，但没有被纳入生命周期管理”。

### 6.2 生命周期管理的核心原则

更合理的代码会显式规定：

- 同一时间只允许一个 `worker`
- 在开始时创建对象
- 先连接信号，再启动任务
- 完成和取消都走统一收尾
- 页面销毁时主动做清理

例如：

```cpp
void startExport()
{
    if (m_worker) {
        return;
    }

    m_worker = new ExportWorker(this);

    connect(m_worker, &ExportWorker::progressChanged,
            this, &ExportPage::onProgressChanged);
    connect(m_worker, &ExportWorker::finished,
            this, &ExportPage::onFinished);
    connect(m_worker, &ExportWorker::cancelled,
            this, &ExportPage::onCancelled);

    m_startButton->setEnabled(false);
    m_cancelButton->setEnabled(true);
    m_statusLabel->setText("导出中...");

    m_worker->start();
}
```

收尾建议统一收口到类似这样的函数：

- `stopAndCleanupWorker()`

这样才能保证：

- 按钮状态恢复一致
- 指针清空一致
- 对象释放一致

一句话总结：  
生命周期管理的本质，是把“对象和任务如何开始、存活、结束、清理”变成明确规则。

---

## 7. 模块边界为什么重要

很多人觉得“我已经分层了，边界还有什么意义”。  
实际上：

- 分层告诉你“住在哪一层”
- 边界告诉你“手能伸到哪里”

举个例子，某个 `Workflow` 在业务层没错，但如果它开始直接：

- `ui->button->setEnabled(false)`
- `QMessageBox::warning(...)`

那说明它越界了。  
这种代码短期可能方便，长期一定会导致：

- 业务逻辑和界面耦合
- 难复用
- 难测试
- 改 UI 时牵连流程逻辑

所以边界的实际应用场景非常明确：  
它不是为了概念完整，而是为了限制职责扩散。

---

## 8. 可扩展设计怎么判断

### 8.1 不可扩展的做法

如果主窗口直接写死：

```cpp
if (toolName == "B") {
    startB();
} else if (toolName == "C") {
    startC();
} else if (toolName == "D") {
    startD();
}
```

同时把这些也都写死：

- 页签标题
- 图标
- exe 路径
- 参数规则

那新增一个 `E.exe` 时，你往往得修改：

- 左侧按钮
- 主窗口逻辑
- 启动逻辑
- 参数逻辑
- 标题逻辑

这说明扩展成本很高。

### 8.2 更可扩展的做法

把工具信息收口成注册结构：

```cpp
struct ToolInfo
{
    QString id;
    QString title;
    QString exePath;
    QString iconPath;
    QStringList defaultArgs;
};
```

主流程只依赖抽象后的注册信息：

```cpp
ToolInfo info = registry->toolInfo(toolId);
sessionManager->openTool(info);
```

这样新增一个工具往往只需要：

- 新增一条配置

而不是改整条主流程。

### 8.3 判断标准

你写完一个功能后可以问自己：

- 新增一个同类对象，要改几个地方
- 替换底层实现，要不要改 UI
- 多开一个实例，当前结构扛不扛得住

如果答案总是“得改主干代码”，那扩展性大概率不够。

---

## 9. 可观测性不只是“打日志”

把可观测性简单理解成日志，并没有错，但还不够完整。

### 9.1 日志只是基础层

日志回答的是：

- 发生了什么

### 9.2 错误上下文让问题可定位

错误上下文回答的是：

- 是哪个工具报错
- 用了什么参数
- 当前处于哪个状态
- 调的是哪一步失败

### 9.3 状态可见让问题可解释

状态可见回答的是：

- 用户现在看到的界面为什么是这样
- 当前是否允许继续操作
- 系统是否还在执行中

### 9.4 指标让问题可量化

指标回答的是：

- 启动耗时多长
- 页面加载多慢
- 某个操作失败率多高
- 某段逻辑触发频率如何

所以更准确地说：

> 日志是可观测性的一部分，可观测性比日志更强调结构化、可定位、可追踪、可量化。

---

## 10. 继续提升时要补上的高阶能力

### 10.1 复杂度控制

你要能判断：

- 什么时候该拆
- 什么时候别拆
- 拆到几层合适
- 当前是不是已经过度抽象

目标是避免“为了架构而架构”。

### 10.2 渐进式演进能力

典型路径是：

- 先能跑
- 再收口
- 再抽流程
- 再抽服务

架构通常不是一次设计完，而是随着复杂度增长逐步长出来的。

### 10.3 异步与并发思维

重点关注：

- 回调会不会打到已销毁对象
- 并发任务时状态会不会冲突
- UI 更新是否跨线程

这在 Qt 客户端里非常关键。

### 10.4 资源与性能意识

重点关注：

- 哪些对象该常驻
- 哪些对象该及时释放
- 哪些 UI 更新过于频繁
- 哪些场景适合缓存或懒加载

目标是不只是能跑，还要跑得住。

### 10.5 错误处理设计

要能判断：

- 错误该在底层消化还是向上抛
- 用户该看到什么提示
- 是否允许重试
- 是否需要回滚状态

目标是系统出错时有秩序，而不是四处弹框。

### 10.6 组件化思维

要能判断：

- 哪些 UI 部件值得做成独立组件
- 哪些是页面私有逻辑
- 组件边界应该划在哪里

目标是让页面不会无限膨胀。

### 10.7 平台化思维

如果你做的是“承载多个工具 exe 的 A”，那就要逐渐从“单页面思维”切换到“平台思维”。

常见关键词包括：

- `Shell`
- `Session`
- `Registry`
- `Tool Runtime`
- 插件化

目标是做“工具宿主平台”，而不是“越来越大的页面”。

### 10.8 领域建模能力

要能判断：

- 真实业务里哪些对象最核心
- 哪些概念应该进入代码模型
- 哪些只是 UI 表现，不该进入业务模型

目标是让系统结构贴近业务，而不是贴近按钮。

### 10.9 团队可维护性思维

要能判断：

- 别人接手时能不能看懂
- 出问题时别人能不能快速定位
- 模块职责是否清晰

代码不是只服务自己。

### 10.10 版本演进与兼容意识

要能判断：

- 老版本数据还能不能打开
- 新功能会不会打坏旧行为
- 替换实现时是否保留旧接口语义

目标是系统能连续演进，而不是频繁推倒重来。

### 10.11 可测试性设计

要能判断：

- 这段逻辑能不能脱离界面测试
- 这个流程是不是必须点 UI 才能验证
- 哪些边界适合 `mock`

好的结构不只是“看着清晰”，还应该“容易验证”。

### 10.12 架构取舍能力

这是高阶能力里最关键的一项。  
你要持续判断：

- 什么地方值得做
- 什么地方现在不值得做
- 什么时候该收敛
- 什么时候该留扩展点
- 什么时候先简单实现

真正成熟的架构能力，不是背了很多原则，而是知道什么时候用、什么时候不用。

---

## 11. 适合 Qt 客户端研发的实用落地模型

对复杂页面，可以先用一个朴素但很有效的三层模型理解：

### UI 层

负责：

- 展示界面
- 响应点击
- 绑定信号槽
- 根据状态刷新控件

不负责：

- 复杂业务流程
- 进程 / 文件 / 网络细节

### 业务层

负责：

- 编排流程
- 管理状态
- 校验输入
- 调用服务
- 输出结果

不负责：

- 直接操作具体控件
- 处理底层系统细节

### 服务层

负责：

- 外部进程执行
- 文件读写
- 网络访问
- 配置读取
- 系统接口调用

不负责：

- 决定界面如何展示
- 决定用户提示文案

这个模型足够覆盖大多数中等复杂度的 Qt 页面。

---

## 12. 一页式结论

如果把“架构思维”压缩成最实用的一句话，就是：

> 让代码在职责、状态、生命周期、扩展、排障这几个方面长期可控。

你可以用下面这份最小检查表做自查：

- 这个页面的 UI 和业务有没有混成一坨
- 当前状态是否清楚、是否有单一修改入口
- 异步对象和后台任务的生命周期是否有人管
- 每个类的职责边界是否清楚
- 新增一个同类功能要改几个地方
- 出问题后是否有足够信息定位

如果这 6 个问题你都能稳定回答，说明你的架构思维已经开始真正落地，而不只是停留在概念层。

